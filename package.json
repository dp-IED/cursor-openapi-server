{
  "name": "cursor-acp-api",
  "version": "3.0.0",
  "type": "module",
  "private": true,
  "description": "OpenAI-compatible REST API for cursor-agent inference (Bun native HTTP)",
  "license": "MIT",
  "dependencies": {
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "start": "bun run server.js",
    "dev": "bun --watch server.js"
  }
}
